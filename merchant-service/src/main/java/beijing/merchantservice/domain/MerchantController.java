package beijing.merchantservice.domain;

public class MerchantController {
	
	public void requestTransaction(String merchantid,String tokenid,
			int amount) {
		//Send the tokenid to the token manager
		
		
		// Get back if it is valid or not
		// If not valid then return not valid
		
		
		// If valid then send the merchantid and token id to
		// the payment service and get back if the money was
		// received
	}

}
